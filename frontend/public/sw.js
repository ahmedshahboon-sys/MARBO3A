const VERSION="marbo3a-shell-v19-live-guest-sync";
const OFFLINE="/offline.html";
const SHELL=[OFFLINE,"/manifest.webmanifest","/favicon.svg","/logo.svg","/pwa-icon.svg","/pwa-maskable.svg","/pwa-192.png","/pwa-512.png","/pwa-maskable-512.png","/apple-touch-icon.png","/brand/marbo3a-app-icon.svg","/brand/marbo3a-symbol-orange.svg"];
self.addEventListener("install",event=>{event.waitUntil(caches.open(VERSION).then(c=>c.addAll(SHELL)));});
self.addEventListener("activate",event=>{event.waitUntil(caches.keys().then(keys=>Promise.all(keys.filter(k=>k!==VERSION).map(k=>caches.delete(k)))).then(()=>self.clients.claim()));});
self.addEventListener("message",event=>{if(event.data?.type==="SKIP_WAITING")self.skipWaiting();});
self.addEventListener("fetch",event=>{const req=event.request;if(req.method!=="GET")return;const url=new URL(req.url);if(url.origin!==location.origin)return;if(url.pathname.startsWith("/api/")||url.pathname.startsWith("/uploads/"))return;if(req.mode==="navigate"){event.respondWith(fetch(req,{cache:"no-store"}).catch(()=>caches.match(OFFLINE)));return}if(url.pathname.startsWith("/_next/static/")){event.respondWith(fetch(req).then(res=>{if(res.ok&&res.type==="basic"){const copy=res.clone();caches.open(VERSION).then(c=>c.put(req,copy)).catch(()=>{})}return res}).catch(()=>caches.match(req)));return}if(SHELL.includes(url.pathname)){event.respondWith(fetch(req,{cache:"no-store"}).then(res=>{if(res.ok&&res.type==="basic"){const copy=res.clone();caches.open(VERSION).then(c=>c.put(req,copy)).catch(()=>{})}return res}).catch(()=>caches.match(req)));}});
self.addEventListener("push",event=>{let data={};try{data=event.data?.json()||{}}catch{data={title:"مربوعة",body:event.data?.text()||"عندك تنبيه جديد"}}event.waitUntil((async()=>{if("setAppBadge" in self.navigator&&Number.isFinite(Number(data.badgeCount)))self.navigator.setAppBadge(Number(data.badgeCount)).catch(()=>{});await self.registration.showNotification(data.title||"مربوعة",{body:data.body||"عندك تنبيه جديد",icon:"/pwa-192.png",badge:"/pwa-192.png",dir:"rtl",lang:"ar",data:{url:data.url||"/home"},tag:data.tag||`marbo3a-${Date.now()}`,renotify:Boolean(data.renotify)})})());});
self.addEventListener("notificationclick",event=>{event.notification.close();const url=event.notification.data?.url||"/home";event.waitUntil(clients.matchAll({type:"window",includeUncontrolled:true}).then(list=>{for(const c of list){if("focus" in c){c.navigate?.(url);return c.focus()}}return clients.openWindow(url)}));});
