"use client";
import {useEffect,useState} from "react";
const token=()=>localStorage.getItem("marbo3a_token")||sessionStorage.getItem("marbo3a_token")||"";
const fmt=n=>{if(n==null)return"-";const u=["B","KB","MB","GB","TB"];let i=0,v=Number(n);while(v>=1024&&i<u.length-1){v/=1024;i++}return`${v.toFixed(i?1:0)} ${u[i]}`};
export default function SystemPage(){const[data,setData]=useState(null),[error,setError]=useState("");async function load(){try{const r=await fetch("/api/admin/system",{headers:{"x-marbo3a-session-mode":"cookie"},cache:"no-store"});const d=await r.json();if(!r.ok)throw new Error(d.error);setData(d.system);setError("")}catch(e){setError(e.message)}}useEffect(()=>{load();const t=setInterval(load,15000);return()=>clearInterval(t)},[]);return <main className="feature-page"><section className="feature-card"><header className="feature-head"><div><small>SUPER ADMIN</small><h1>حالة السيرفر</h1></div><a href="/">رجوع</a></header>{error&&<div className="settings-status">{error}</div>}{data&&<div className="feature-grid"><div className="feature-block"><h3>العملية</h3><p>Uptime: {Math.round(data.uptimeSeconds/60)} دقيقة</p><p>Node: {data.node}</p><p>Redis: {data.redis?"متصل":"غير متصل"}</p><p>Push: {data.push?"مفعّل":"يحتاج VAPID"}</p></div><div className="feature-block"><h3>الذاكرة</h3><p>RSS: {fmt(data.memory?.rss)}</p><p>Heap used: {fmt(data.memory?.heapUsed)}</p><p>Heap total: {fmt(data.memory?.heapTotal)}</p></div><div className="feature-block"><h3>قاعدة البيانات</h3><p>الحجم: {fmt(data.database?.db_bytes)}</p><p>{data.database?.now&&new Date(data.database.now).toLocaleString("ar-LY")}</p></div><div className="feature-block"><h3>القرص</h3><p>الإجمالي: {fmt(data.disk?.total)}</p><p>المتاح: {fmt(data.disk?.free)}</p></div></div>}</section></main>}
