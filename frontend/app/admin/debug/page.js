"use client";
import {useEffect,useState} from "react";
const tok=()=>localStorage.getItem("marbo3a_token")||sessionStorage.getItem("marbo3a_token")||"";
async function api(p){const r=await fetch(p,{headers:{authorization:`Bearer ${tok()}`},cache:"no-store"});const d=await r.json();if(!r.ok)throw new Error(d.error||"FAILED");return d}
export default function Debug(){const[list,setList]=useState([]),[sel,setSel]=useState(null),[err,setErr]=useState("");async function load(){try{setList((await api("/api/debug/sessions")).sessions||[])}catch{setErr("تعذر تحميل جلسات الديباق")}}async function open(id){try{setSel(await api(`/api/debug/sessions/${id}`))}catch{setErr("تعذر فتح الجلسة")}}useEffect(()=>{load();const t=setInterval(load,5000);return()=>clearInterval(t)},[]);return <main className="debug-admin-page" dir="rtl"><header><div><small>ADMIN ONLY</small><h1>ديباق مربوعة</h1><p>تسجيل حي للحركات والأخطاء ومشاكل الواجهة بدون كلمات سر أو Tokens.</p></div><a href="/admin">لوحة الإدارة</a></header>{err&&<div className="debug-error">{err}</div>}<section className="debug-grid"><aside><h2>الجلسات</h2>{list.map(s=><button key={s.id} className={sel?.session?.id===s.id?"active":""} onClick={()=>open(s.id)}><b>{new Date(s.started_at).toLocaleString("ar-LY")}</b><span>{s.event_count} حركة · {s.error_count} مشكلة</span><code>{s.id.slice(0,8)}</code></button>)}</aside><article>{!sel?<div className="debug-empty">افتح جلسة لمراجعة ما حدث.</div>:<><div className="debug-summary"><div><b>{sel.summary?.errors||0}</b><span>أخطاء</span></div><div><b>{sel.summary?.api||0}</b><span>API</span></div><div><b>{sel.summary?.ui||0}</b><span>واجهة</span></div><div><b>{sel.summary?.clicks||0}</b><span>ضغطات</span></div></div><h2>Session {sel.session.id}</h2><div className="debug-events">{(sel.events||[]).map(e=><div key={e.id} className={`debug-event ${/error|overlap|overflow/.test(e.event_type)?"bad":""}`}><time>{new Date(e.created_at).toLocaleTimeString("ar-LY")}</time><b>{e.event_type}</b><span>{e.path}</span><pre>{JSON.stringify(e.payload,null,2)}</pre></div>)}</div></>}</article></section></main>}
