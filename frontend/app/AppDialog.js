"use client";
import {useEffect} from "react";
export default function AppDialog({open,title,description,value,onValueChange,input=false,confirmLabel="تأكيد",cancelLabel="إلغاء",danger=false,onConfirm,onClose,children}){useEffect(()=>{if(!open)return;const k=e=>e.key==="Escape"&&onClose?.();document.addEventListener("keydown",k);return()=>document.removeEventListener("keydown",k)},[open,onClose]);if(!open)return null;return <div className="app-dialog-backdrop" role="presentation" onMouseDown={e=>e.target===e.currentTarget&&onClose?.()}><section className="app-dialog" role="dialog" aria-modal="true" aria-labelledby="app-dialog-title"><h3 id="app-dialog-title">{title}</h3>{description&&<p>{description}</p>}{input&&<textarea autoFocus value={value||""} onChange={e=>onValueChange?.(e.target.value)} maxLength={3000}/>} {children}<footer><button type="button" className="secondary" onClick={onClose}>{cancelLabel}</button><button type="button" className={danger?"danger":"primary"} onClick={onConfirm}>{confirmLabel}</button></footer></section></div>}
