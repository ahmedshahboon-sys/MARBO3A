"use client";
import {useEffect,useState} from "react";
const token=()=>localStorage.getItem("marbo3a_token")||sessionStorage.getItem("marbo3a_token")||"";
export default function MapPage(){const[cities,setCities]=useState([]),[error,setError]=useState("");useEffect(()=>{fetch("/api/map/cities",{headers:{authorization:`Bearer ${token()}`}}).then(async r=>{const d=await r.json();if(!r.ok)throw new Error(d.error);setCities(d.cities||[])}).catch(e=>setError(e.message));},[]);return <main className="feature-page"><section className="feature-card"><header className="feature-head"><div><small>مربوعة • الخريطة</small><h1>خريطة المجتمع</h1></div><a href="/">رجوع</a></header><p style={{color:"#9aa3b1",fontSize:12}}>نعرض المدن والتجمعات فقط. الموقع الدقيق ما يظهرش للناس إلا وفق إعدادات الخصوصية.</p>{error&&<div className="settings-status">{error}</div>}<div className="map-shell"><div className="map-grid"/><div className="map-points">{cities.map(c=><div className="map-point" key={c.city}><b>{c.city}</b><small>{c.users} مستخدم</small></div>)}{!cities.length&&!error&&<div className="map-point">ما فيش مدن مشاركة توا</div>}</div></div></section></main>}
