"use client";
import {useEffect,useMemo,useState} from "react";
import Link from "next/link";
import {usePathname} from "next/navigation";
import Icon from "./Icon";
import {UI_MODE} from "./ui-mode";
const getToken=()=>typeof window==="undefined"?"":(localStorage.getItem("marbo3a_token")||sessionStorage.getItem("marbo3a_token")||"");
export default function SocialDock(){const path=usePathname(),[visible,setVisible]=useState(false),[unread,setUnread]=useState(0);useEffect(()=>{const t=getToken();setVisible(Boolean(t));if(!t){setUnread(0);return}let alive=true,busy=false;async function refresh(){if(!alive||busy||document.visibilityState!=="visible")return;busy=true;try{const r=await fetch("/api/chats",{headers:{authorization:`Bearer ${getToken()}`},cache:"no-store"});if(!r.ok)return;const d=await r.json();if(alive)setUnread((d.chats||[]).reduce((n,c)=>n+Number(c.unread_count||0),0))}catch{}finally{busy=false}}refresh();const timer=setInterval(refresh,15000),onVisible=()=>document.visibilityState==="visible"&&refresh();document.addEventListener("visibilitychange",onVisible);return()=>{alive=false;clearInterval(timer);document.removeEventListener("visibilitychange",onVisible)}},[path]);const badge=useMemo(()=>unread>99?"99+":String(unread),[unread]);if(!visible||path?.startsWith("/admin")||path==="/privacy"||path==="/terms")return null;const active=p=>path===p||path?.startsWith(p+"/");const createLabel=UI_MODE==="v3"?"إنشاء":"نشر";return <nav className="social-dock" aria-label="التنقل الرئيسي"><div className="social-dock-track"><Link prefetch href="/home" className={active("/home")?"active":""}><Icon name="home"/><span>الرئيسية</span></Link><Link prefetch href="/friends" className={active("/friends")?"active":""}><Icon name="users"/><span>الأصحاب</span></Link><Link prefetch href="/home#compose" className="dock-create" aria-label="إنشاء منشور"><Icon name="sparkles"/><span>{createLabel}</span></Link><Link prefetch href="/messages" className={`dock-messages ${active("/messages")||active("/chat")?"active":""}`}><span className="dock-icon-wrap"><Icon name="message"/>{unread>0&&<b className="dock-unread-badge">{badge}</b>}</span><span>الرسائل</span></Link><Link prefetch href="/rooms" className={active("/rooms")||active("/room")?"active":""}><Icon name="hash"/><span>الغرف</span></Link></div></nav>}
