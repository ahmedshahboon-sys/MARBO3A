"use client";
import {useParams} from "next/navigation";
import {useState} from "react";
const saved=()=>localStorage.getItem("marbo3a_token")||sessionStorage.getItem("marbo3a_token")||"";
const human=e=>({INVITE_INVALID:"رابط الدعوة غير صالح أو انتهت صلاحيته",ROOM_BANNED:"أنت محظور من هذه الغرفة",UNAUTHORIZED:"سجل دخولك أولًا"}[e]||e||"تعذر استخدام الدعوة");
export default function Invite(){const{token}=useParams(),[status,setStatus]=useState(""),[busy,setBusy]=useState(false);async function join(){const t=saved();if(!t){const next=`/invite/${encodeURIComponent(token)}`;location.href=`/?next=${encodeURIComponent(next)}`;return;}setBusy(true);try{const r=await fetch(`/api/invites/${token}/redeem`,{method:"POST",headers:{authorization:`Bearer ${t}`},cache:"no-store"});const d=await r.json().catch(()=>({}));if(!r.ok){setStatus(human(d.error));return;}setStatus("تم الانضمام للغرفة بنجاح");const roomId=Number(d.roomId||d.room_id||d.room?.id);setTimeout(()=>location.replace(roomId?`/room/${roomId}/chat`:"/rooms"),450)}catch{setStatus("تعذر استخدام الدعوة الآن")}finally{setBusy(false)}}return <main className="feature-page"><section className="feature-card invite-card"><img src="/logo.svg" width="72" height="72" alt="مربوعة"/><h1>دعوة إلى غرفة في مربوعة</h1><p>اضغط للانضمام. إذا مش مسجل دخول بنرجعك للدعوة بعد تسجيل الدخول.</p><div className="invite-token">{token}</div><button className="feature-action" onClick={join} disabled={busy}>{busy?"جاري الانضمام...":"قبول الدعوة"}</button>{status&&<div className="settings-status" role="status">{status}</div>}</section></main>}
