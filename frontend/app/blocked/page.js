"use client";
import {useEffect,useState} from "react";
const token=()=>localStorage.getItem("marbo3a_token")||sessionStorage.getItem("marbo3a_token")||"";
async function api(path,options={}){const r=await fetch(path,{...options,headers:{authorization:`Bearer ${token()}`,...(options.headers||{})}});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||"REQUEST_FAILED");return d;}
export default function Blocked(){const[list,setList]=useState([]),[status,setStatus]=useState("");async function load(){try{setList((await api("/api/blocks")).blocks||[])}catch(e){setStatus(e.message)}}useEffect(()=>{load()},[]);async function unblock(id){try{await api(`/api/users/${id}/block`,{method:"DELETE"});await load()}catch(e){setStatus(e.message)}}return <main className="feature-page"><section className="feature-card"><header className="feature-head"><div><small>الخصوصية</small><h1>الحسابات المحظورة</h1></div><a href="/">رجوع</a></header>{status&&<div className="settings-status">{status}</div>}<div className="feature-list">{list.map(x=><article key={x.blocked_id}><b>{x.display_name}</b><span>@{x.username}</span><div className="feature-actions"><button onClick={()=>unblock(x.blocked_id)}>فك الحظر</button></div></article>)}{!list.length&&<article>ما عندكش حسابات محظورة.</article>}</div></section></main>}
