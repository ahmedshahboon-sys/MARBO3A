"use client";
import {useEffect,useState} from "react";
import Icon from "../Icon";
const token=()=>localStorage.getItem("marbo3a_token")||sessionStorage.getItem("marbo3a_token")||"";
async function api(path,options={}){const headers={authorization:`Bearer ${token()}`,...(options.headers||{})};if(options.body)headers["content-type"]="application/json";const r=await fetch(path,{...options,headers});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||"REQUEST_FAILED");return d}
export default function Notifications(){const[items,setItems]=useState([]),[error,setError]=useState("");useEffect(()=>{if(!token()){location.href="/";return}Promise.all([api("/api/notifications"),api("/api/notifications/read",{method:"POST"})]).then(([d])=>setItems(d.notifications||[])).catch(()=>setError("تعذر تحميل التنبيهات"))},[]);return <main className="social-page"><div className="social-shell"><header className="sf-top"><a className="sf-brand" href="/feed"><img src="/logo.svg"/><span><b>التنبيهات</b><small>MARBO3A</small></span></a><div className="sf-top-actions"><a href="/feed"><Icon name="home"/></a></div></header><div className="sf-simple-page"><section className="sf-panel"><h1>التنبيهات</h1>{error&&<div className="sf-alert">{error}</div>}<div className="sf-people-list">{items.map(n=><article key={n.id}><span className="sf-room-icon"><Icon name="bell"/></span><div style={{flex:1}}><b>{n.title}</b>{n.body&&<p className="sf-muted" style={{margin:"4px 0"}}>{n.body}</p>}<small>{new Date(n.created_at).toLocaleString("ar-LY")}</small></div></article>)}{!items.length&&!error&&<div className="sf-empty"><Icon name="bell"/><h2>ما فيش تنبيهات جديدة</h2><p>أي طلب صداقة أو رسالة أو حدث مهم بيظهر هنا.</p></div>}</div></section></div></div></main>}
