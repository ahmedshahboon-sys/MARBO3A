"use client";
import {useEffect,useState} from "react";
import {useRouter} from "next/navigation";
import Icon from "../Icon";
const token=()=>localStorage.getItem("marbo3a_token")||sessionStorage.getItem("marbo3a_token")||"";
async function api(path,options={}){const headers={authorization:`Bearer ${token()}`,...(options.headers||{})};if(options.body)headers["content-type"]="application/json";const r=await fetch(path,{...options,headers});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||"REQUEST_FAILED");return d}
const href=n=>n.type==="friend_request"?"/friends":n.type?.startsWith("post_")&&n.ref_id?`/home?post=${n.ref_id}`:n.type?.includes("message")?"/messages":n.type?.includes("room")?"/rooms":"/notifications";const icon=n=>n.type==="friend_request"?"users":n.type?.startsWith("post_")?"heart":n.type?.includes("message")?"message":n.type?.includes("room")?"hash":"bell";const when=v=>{const t=new Date(v).getTime();if(!Number.isFinite(t))return"";const s=Math.max(0,Math.floor((Date.now()-t)/1000));if(s<60)return"الآن";if(s<3600)return`${Math.floor(s/60)} د`;if(s<86400)return`${Math.floor(s/3600)} س`;if(s<172800)return"أمس";if(s<604800)return`${Math.floor(s/86400)} ي`;const d=new Date(t);return`${d.getDate()}/${d.getMonth()+1}/${String(d.getFullYear()).slice(-2)}`};
export default function Notifications(){const router=useRouter(),[items,setItems]=useState([]),[error,setError]=useState(""),[busy,setBusy]=useState(false);async function load(){try{setItems((await api("/api/notifications")).notifications||[])}catch{setError("تعذر تحميل التنبيهات")}}useEffect(()=>{if(!token()){location.href="/";return}load()},[]);async function open(n){try{if(!n.read_at)await api(`/api/notifications/${n.id}/read`,{method:"PUT"})}catch{}router.push(href(n))}async function markAll(){setBusy(true);try{await api("/api/notifications/read-all",{method:"PUT"});setItems(a=>a.map(x=>({...x,read_at:x.read_at||new Date().toISOString()})))}catch{setError("تعذر تعليم التنبيهات كمقروءة")}finally{setBusy(false)}}const unread=items.filter(x=>!x.read_at).length;return <main className="social-page"><div className="social-shell"><header className="sf-top"><a className="sf-brand" href="/home"><img src="/logo.svg" alt="مربوعة"/><span><b>التنبيهات</b><small>MARBO3A</small></span></a></header><div className="sf-simple-page"><section className="sf-panel notifications-panel"><div className="notif-head"><div><h1>التنبيهات</h1><p>{unread?`${unread} تنبيه غير مقروء`:"أنت مطّلع على كل شيء"}</p></div>{unread>0&&<button onClick={markAll} disabled={busy}>{busy?"جاري...":"تعليم الكل كمقروء"}</button>}</div>{error&&<div className="sf-alert" onClick={()=>setError("")}>{error}</div>}<div className="notification-list">{items.map(n=><button key={n.id} onClick={()=>open(n)} className={`notification-row ${!n.read_at?"unread":""}`}><span className="notif-icon"><Icon name={icon(n)}/></span><div><b>{n.title}</b>{n.body&&<p>{n.body}</p>}<small title={new Date(n.created_at).toLocaleString("ar-LY")}>{when(n.created_at)}</small></div>{!n.read_at&&<i aria-label="غير مقروء"/>}</button>)}{!items.length&&!error&&<div className="sf-empty"><Icon name="bell"/><h2>ما فيش تنبيهات توا</h2><p>طلبات الصداقة، الرسائل والتفاعلات بيظهروا هنا.</p></div>}</div></section></div></div></main>}
