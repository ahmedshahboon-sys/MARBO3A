"use client";
import {useEffect,useState} from "react";
import {useParams} from "next/navigation";
import ShareQR from "../../ShareQR";
import Icon from "../../Icon";
const token=()=>typeof window!=="undefined"?(localStorage.getItem("marbo3a_token")||sessionStorage.getItem("marbo3a_token")||""):"";
async function api(path,options={}){const headers={...(options.headers||{})};const t=token();if(t)headers.authorization=`Bearer ${t}`;if(options.body)headers["content-type"]="application/json";const r=await fetch(path,{...options,headers});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||"REQUEST_FAILED");return d}
function Avatar({u,size=92}){return u?.avatar_url?<img className="sf-avatar" width={size} height={size} src={u.avatar_url} alt=""/>:<span className="sf-avatar sf-avatar-fallback" style={{width:size,height:size}}>{u?.display_name?.[0]||"م"}</span>}
export default function PublicUser(){const{username}=useParams(),[profile,setProfile]=useState(null),[posts,setPosts]=useState([]),[error,setError]=useState(""),[status,setStatus]=useState("");async function load(){try{const d=token()?await api(`/api/social/profile/${encodeURIComponent(username)}`):await api(`/api/public/profile/${encodeURIComponent(username)}`);setProfile(d.profile||d.user);setPosts(d.posts||[])}catch{setError("الحساب غير موجود أو غير متاح")}}useEffect(()=>{if(username)load()},[username]);async function add(){try{await api("/api/friends/request",{method:"POST",body:JSON.stringify({userId:profile.id})});setStatus("تم إرسال طلب الصداقة");await load()}catch(e){setStatus(e.message==="RELATION_EXISTS"?"الطلب أو الصداقة موجودة بالفعل":"تعذر إرسال الطلب")}}async function chat(){try{const d=await api(`/api/chats/with/${profile.id}`,{method:"POST"});location.href=`/chat/${d.conversation.id}`}catch{setStatus("تعذر فتح المحادثة الآن")}}const url=typeof window!=="undefined"?window.location.href:`https://marbo3a.ly/u/${username}`;if(error)return <main className="social-page"><div className="sf-simple-page"><div className="sf-alert">{error}</div></div></main>;if(!profile)return <main className="social-page"><div className="sf-simple-page"><div className="sf-skeleton">جاري تحميل الحساب...</div></div></main>;return <main className="social-page"><div className="social-shell"><header className="sf-top"><a className="sf-brand" href="/feed"><img src="/logo.svg"/><span><b>مربوعة</b><small>PROFILE</small></span></a><div className="sf-top-actions"><a href="/feed"><Icon name="home"/></a><a href="/messages"><Icon name="message"/></a></div></header><div className="sf-profile-page"><section className="sf-profile-card"><div className="sf-cover" style={profile.cover_url?{backgroundImage:`url(${profile.cover_url})`}:{}}></div><div className="sf-profile-body"><div className="sf-profile-avatar"><Avatar u={profile}/>{profile.online&&<span className="sf-online-dot"/>}</div><div className="sf-profile-main"><h1>{profile.display_name}</h1><p className="sf-muted">@{profile.username}</p>{profile.bio&&<p className="sf-profile-bio">{profile.bio}</p>}{profile.city&&<p className="sf-muted">{profile.city}</p>}<div className="sf-profile-counts"><span><b>{profile.posts_count??posts.length}</b> منشور</span><span><b>{profile.friends_count??0}</b> صديق</span></div></div><div className="sf-profile-actions">{profile.relation==="self"?<a className="sf-primary" href="/profile/edit">تعديل الملف</a>:<><button className="sf-primary" onClick={profile.relation==="friend"?chat:add}>{profile.relation==="friend"?"مراسلة":"إضافة صديق"}</button>{profile.relation!=="friend"&&<button className="ghost" onClick={chat}>مراسلة</button>}</>}<ShareQR url={url} title={`${profile.display_name} على مربوعة`} label="مشاركة"/></div></div>{status&&<div className="sf-alert">{status}</div>}</section><section className="sf-profile-feed"><h2>المنشورات</h2>{posts.map(p=><article className="sf-post" key={p.id}><div className="sf-post-head"><div className="sf-user"><Avatar u={profile} size={42}/><span><b>{profile.display_name}</b><small>@{profile.username} · {new Date(p.created_at).toLocaleDateString("ar-LY")}</small></span></div></div>{p.body&&<p className="sf-body">{p.body}</p>}{p.image_url&&<img className="sf-post-image" src={p.image_url}/>}<div className="sf-post-stats"><span>{p.likes_count||0} إعجاب</span><span>{p.comments_count||0} تعليق</span></div></article>)}{!posts.length&&<div className="sf-empty"><Icon name="sparkles"/><h2>ما فيش منشورات حتى الآن</h2></div>}</section></div></div></main>}
