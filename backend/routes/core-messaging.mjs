import {pool,redis,requireAuth,isAdmin,emitRoom,emitChat,clean} from "../runtime.mjs";

export function registerCoreMessaging(app){
  app.get("/api/rooms/:id/messages",async(req,res)=>{const u=await requireAuth(req,res);if(!u)return;const id=Number(req.params.id);if(!(await pool.query(`SELECT 1 FROM room_members WHERE room_id=$1 AND user_id=$2`,[id,u.id])).rows[0]&&!isAdmin(u))return res.status(403).json({ok:false,error:"NOT_ROOM_MEMBER"});const rows=(await pool.query(`SELECT m.id,m.room_id,m.body,m.created_at,m.edited_at,m.deleted_at,m.forwarded_from_message_id,x.id user_id,x.username,x.display_name,x.gender FROM messages m JOIN users x ON x.id=m.user_id WHERE m.room_id=$1 ORDER BY m.id DESC LIMIT 100`,[id])).rows.reverse();res.json({ok:true,messages:rows})});
  app.post("/api/rooms/:id/messages",async(req,res)=>{const u=await requireAuth(req,res);if(!u)return;const id=Number(req.params.id),body=clean(req.body?.body,1000);if(!body)return res.status(400).json({ok:false,error:"EMPTY_MESSAGE"});if(!(await pool.query(`SELECT 1 FROM room_members WHERE room_id=$1 AND user_id=$2`,[id,u.id])).rows[0])return res.status(403).json({ok:false,error:"NOT_ROOM_MEMBER"});const key=`msg:rate:${u.id}`,n=await redis.incr(key);if(n===1)await redis.expire(key,30);if(n>20)return res.status(429).json({ok:false,error:"MESSAGE_RATE_LIMIT"});const msg=(await pool.query(`INSERT INTO messages(room_id,user_id,body) VALUES($1,$2,$3) RETURNING id,room_id,body,created_at,edited_at,deleted_at,forwarded_from_message_id`,[id,u.id,body])).rows[0];const enriched={...msg,user_id:u.id,username:u.username,display_name:u.display_name,gender:u.gender};emitRoom(id,"message:new",enriched);res.status(201).json({ok:true,message:enriched})});
  app.patch("/api/messages/:id",async(req,res)=>{const u=await requireAuth(req,res);if(!u)return;const id=Number(req.params.id),body=clean(req.body?.body,1000);if(!body)return res.status(400).json({ok:false,error:"EMPTY_MESSAGE"});const msg=(await pool.query(`SELECT * FROM messages WHERE id=$1`,[id])).rows[0];if(!msg)return res.status(404).json({ok:false,error:"MESSAGE_NOT_FOUND"});if(String(msg.user_id)!==String(u.id)&&!isAdmin(u))return res.status(403).json({ok:false,error:"FORBIDDEN"});const row=(await pool.query(`UPDATE messages SET body=$1,edited_at=NOW(),deleted_at=NULL WHERE id=$2 RETURNING *`,[body,id])).rows[0];emitRoom(row.room_id,"message:updated",row);res.json({ok:true,message:row})});
  app.delete("/api/messages/:id",async(req,res)=>{const u=await requireAuth(req,res);if(!u)return;const id=Number(req.params.id),msg=(await pool.query(`SELECT * FROM messages WHERE id=$1`,[id])).rows[0];if(!msg)return res.status(404).json({ok:false,error:"MESSAGE_NOT_FOUND"});if(String(msg.user_id)!==String(u.id)&&!isAdmin(u))return res.status(403).json({ok:false,error:"FORBIDDEN"});const row=(await pool.query(`UPDATE messages SET body='',deleted_at=NOW() WHERE id=$1 RETURNING id,room_id,deleted_at`,[id])).rows[0];emitRoom(row.room_id,"message:deleted",row);res.json({ok:true,message:row})});
  app.post("/api/messages/:id/forward",async(req,res)=>{const u=await requireAuth(req,res);if(!u)return;const source=(await pool.query(`SELECT * FROM messages WHERE id=$1 AND deleted_at IS NULL`,[Number(req.params.id)])).rows[0];if(!source)return res.status(404).json({ok:false,error:"MESSAGE_NOT_FOUND"});const roomId=Number(req.body?.roomId);if(!Number.isInteger(roomId))return res.status(400).json({ok:false,error:"INVALID_ROOM"});if(!(await pool.query(`SELECT 1 FROM room_members WHERE room_id=$1 AND user_id=$2`,[roomId,u.id])).rows[0])return res.status(403).json({ok:false,error:"NOT_ROOM_MEMBER"});const row=(await pool.query(`INSERT INTO messages(room_id,user_id,body,forwarded_from_message_id) VALUES($1,$2,$3,$4) RETURNING *`,[roomId,u.id,source.body,source.id])).rows[0];res.status(201).json({ok:true,message:row})});

  app.get("/api/chats",async(req,res)=>{const u=await requireAuth(req,res);if(!u)return;const rows=(await pool.query(`SELECT c.id,c.updated_at,x.id user_id,x.username,x.display_name,x.gender,x.avatar_url,(SELECT CASE WHEN dm.deleted_at IS NULL THEN dm.body ELSE 'تم حذف الرسالة' END FROM direct_messages dm WHERE dm.conversation_id=c.id ORDER BY dm.id DESC LIMIT 1) last_message FROM direct_conversations c JOIN users x ON x.id=CASE WHEN c.user1_id=$1 THEN c.user2_id ELSE c.user1_id END WHERE c.user1_id=$1 OR c.user2_id=$1 ORDER BY c.updated_at DESC`,[u.id])).rows;res.json({ok:true,chats:rows})});
  app.post("/api/chats/with/:userId",async(req,res)=>{const u=await requireAuth(req,res);if(!u)return;const target=Number(req.params.userId);if(!Number.isInteger(target)||target===Number(u.id))return res.status(400).json({ok:false,error:"INVALID_USER"});if(!(await pool.query(`SELECT id FROM users WHERE id=$1 AND account_status='active'`,[target])).rows[0])return res.status(404).json({ok:false,error:"USER_NOT_FOUND"});let c=(await pool.query(`SELECT * FROM direct_conversations WHERE LEAST(user1_id,user2_id)=LEAST($1,$2) AND GREATEST(user1_id,user2_id)=GREATEST($1,$2)`,[u.id,target])).rows[0];if(!c)c=(await pool.query(`INSERT INTO direct_conversations(user1_id,user2_id) VALUES($1,$2) RETURNING *`,[u.id,target])).rows[0];res.status(201).json({ok:true,conversation:c})});
  app.get("/api/chats/:id/messages",async(req,res)=>{const u=await requireAuth(req,res);if(!u)return;const id=Number(req.params.id);if(!(await pool.query(`SELECT 1 FROM direct_conversations WHERE id=$1 AND(user1_id=$2 OR user2_id=$2)`,[id,u.id])).rows[0]&&!isAdmin(u))return res.status(403).json({ok:false,error:"FORBIDDEN"});const rows=(await pool.query(`SELECT dm.id,dm.conversation_id,dm.body,dm.created_at,dm.edited_at,dm.deleted_at,dm.forwarded_from_message_id,x.id sender_id,x.username,x.display_name FROM direct_messages dm JOIN users x ON x.id=dm.sender_id WHERE dm.conversation_id=$1 ORDER BY dm.id DESC LIMIT 100`,[id])).rows.reverse();res.json({ok:true,messages:rows})});
  app.post("/api/chats/:id/messages",async(req,res)=>{const u=await requireAuth(req,res);if(!u)return;const id=Number(req.params.id),body=clean(req.body?.body,1000);if(!body)return res.status(400).json({ok:false,error:"EMPTY_MESSAGE"});if(!(await pool.query(`SELECT 1 FROM direct_conversations WHERE id=$1 AND(user1_id=$2 OR user2_id=$2)`,[id,u.id])).rows[0])return res.status(403).json({ok:false,error:"FORBIDDEN"});const row=(await pool.query(`INSERT INTO direct_messages(conversation_id,sender_id,body) VALUES($1,$2,$3) RETURNING *`,[id,u.id,body])).rows[0];await pool.query(`UPDATE direct_conversations SET updated_at=NOW() WHERE id=$1`,[id]);const enriched={...row,sender_id:u.id,username:u.username,display_name:u.display_name};emitChat(id,"direct:new",enriched);res.status(201).json({ok:true,message:enriched})});
  app.patch("/api/direct-messages/:id",async(req,res)=>{const u=await requireAuth(req,res);if(!u)return;const id=Number(req.params.id),body=clean(req.body?.body,1000),m=(await pool.query(`SELECT * FROM direct_messages WHERE id=$1`,[id])).rows[0];if(!m)return res.status(404).json({ok:false,error:"MESSAGE_NOT_FOUND"});if(String(m.sender_id)!==String(u.id)&&!isAdmin(u))return res.status(403).json({ok:false,error:"FORBIDDEN"});const row=(await pool.query(`UPDATE direct_messages SET body=$1,edited_at=NOW(),deleted_at=NULL WHERE id=$2 RETURNING *`,[body,id])).rows[0];emitChat(row.conversation_id,"direct:updated",row);res.json({ok:true,message:row})});
  app.delete("/api/direct-messages/:id",async(req,res)=>{const u=await requireAuth(req,res);if(!u)return;const id=Number(req.params.id),m=(await pool.query(`SELECT * FROM direct_messages WHERE id=$1`,[id])).rows[0];if(!m)return res.status(404).json({ok:false,error:"MESSAGE_NOT_FOUND"});if(String(m.sender_id)!==String(u.id)&&!isAdmin(u))return res.status(403).json({ok:false,error:"FORBIDDEN"});const row=(await pool.query(`UPDATE direct_messages SET body='',deleted_at=NOW() WHERE id=$1 RETURNING id,conversation_id,deleted_at`,[id])).rows[0];emitChat(row.conversation_id,"direct:deleted",row);res.json({ok:true,message:row})});
  app.post("/api/direct-messages/:id/forward",async(req,res)=>{const u=await requireAuth(req,res);if(!u)return;const source=(await pool.query(`SELECT * FROM direct_messages WHERE id=$1 AND deleted_at IS NULL`,[Number(req.params.id)])).rows[0];if(!source)return res.status(404).json({ok:false,error:"MESSAGE_NOT_FOUND"});const conversationId=Number(req.body?.conversationId);if(!Number.isInteger(conversationId))return res.status(400).json({ok:false,error:"INVALID_CHAT"});if(!(await pool.query(`SELECT 1 FROM direct_conversations WHERE id=$1 AND(user1_id=$2 OR user2_id=$2)`,[conversationId,u.id])).rows[0])return res.status(403).json({ok:false,error:"FORBIDDEN"});const row=(await pool.query(`INSERT INTO direct_messages(conversation_id,sender_id,body,forwarded_from_message_id) VALUES($1,$2,$3,$4) RETURNING *`,[conversationId,u.id,source.body,source.id])).rows[0];res.status(201).json({ok:true,message:row})});
}
