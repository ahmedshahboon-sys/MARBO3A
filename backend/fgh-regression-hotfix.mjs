import http from "http";
import {pool,requireAuth,clean,redis} from "./runtime.mjs";

const prior=http.createServer.bind(http);
async function friends(a,b){return Boolean((await pool.query(`SELECT 1 FROM friendships WHERE status='accepted' AND ((requester_id=$1 AND addressee_id=$2) OR (requester_id=$2 AND addressee_id=$1)) LIMIT 1`,[a,b])).rows[0])}
async function mutual(a,b){return Number((await pool.query(`WITH a1 AS(SELECT CASE WHEN requester_id=$1 THEN addressee_id ELSE requester_id END id FROM friendships WHERE status='accepted' AND(requester_id=$1 OR addressee_id=$1)),b1 AS(SELECT CASE WHEN requester_id=$2 THEN addressee_id ELSE requester_id END id FROM friendships WHERE status='accepted' AND(requester_id=$2 OR addressee_id=$2))SELECT COUNT(*)::int n FROM a1 JOIN b1 USING(id)`,[a,b])).rows[0]?.n||0)}
async function allowed(rule,viewer,target){if(String(viewer)===String(target)||rule==="everyone")return true;if(rule==="nobody")return false;if(await friends(viewer,target))return true;return rule==="friends_of_friends"&&(await mutual(viewer,target))>0}
async function relation(viewer,target){if(String(viewer)===String(target))return"self";const r=(await pool.query(`SELECT requester_id,status FROM friendships WHERE ((requester_id=$1 AND addressee_id=$2) OR (requester_id=$2 AND addressee_id=$1)) ORDER BY id DESC LIMIT 1`,[viewer,target])).rows[0];if(!r)return"none";if(r.status==="accepted")return"friend";if(r.status==="pending")return String(r.requester_id)===String(viewer)?"outgoing":"incoming";return"none"}
const postSql=`SELECT p.id,p.body,p.image_url,p.location_label,p.created_at,p.updated_at,u.id user_id,u.username,u.display_name,u.gender,u.avatar_url,COALESCE(pp.who_can_see_posts,'everyone') privacy_rule,(SELECT COUNT(*)::int FROM post_likes l WHERE l.post_id=p.id) likes_count,(SELECT COUNT(*)::int FROM post_comments c WHERE c.post_id=p.id AND c.deleted_at IS NULL) comments_count,EXISTS(SELECT 1 FROM post_likes ml WHERE ml.post_id=p.id AND ml.user_id=$1) liked,COALESCE((SELECT jsonb_agg(jsonb_build_object('url',pm.url,'type',pm.media_type,'position',pm.position) ORDER BY pm.position,pm.id) FROM post_media pm WHERE pm.post_id=p.id),CASE WHEN p.image_url IS NOT NULL AND p.image_url<>'' THEN jsonb_build_array(jsonb_build_object('url',p.image_url,'type','image','position',0)) ELSE '[]'::jsonb END) media FROM posts p JOIN users u ON u.id=p.user_id LEFT JOIN profile_privacy pp ON pp.user_id=u.id WHERE p.deleted_at IS NULL`;
async function filterVisible(rows,viewer){const out=[];for(const row of rows){if(await allowed(row.privacy_rule,viewer,row.user_id)){delete row.privacy_rule;row.relation=await relation(viewer,row.user_id);out.push(row)}}return out}

http.createServer=function fghRegressionHotfix(app,...args){
 if(typeof app==="function"&&app?.use){
  app.get("/api/feed",async(req,res)=>{try{const u=await requireAuth(req,res);if(!u)return;const cursor=Math.max(0,Number(req.query.cursor)||0),limit=Math.min(30,Math.max(5,Number(req.query.limit)||15)),params=[u.id];let cursorSql="";if(cursor){params.push(cursor);cursorSql=` AND p.id<$${params.length}`}params.push(Math.min(120,limit*4));const candidates=(await pool.query(`${postSql}${cursorSql} ORDER BY p.id DESC LIMIT $${params.length}`,params)).rows,visible=(await filterVisible(candidates,u.id)).slice(0,limit);res.json({ok:true,posts:visible,nextCursor:visible.length===limit?visible.at(-1).id:null})}catch(e){console.error("FGH regression feed",e);res.status(500).json({ok:false,error:"FEED_LOAD_FAILED"})}});

  app.get("/api/social/profile/:username",async(req,res)=>{try{const viewer=await requireAuth(req,res);if(!viewer)return;const uname=clean(req.params.username,24).toLowerCase(),p=(await pool.query(`SELECT u.id,u.username,u.display_name,u.gender,u.bio,u.avatar_url,u.cover_url,u.last_seen_at,u.created_at,u.birth_date,u.pinned_post_id,l.city,COALESCE(pp.show_last_seen,TRUE) show_last_seen,COALESCE(pp.show_city,TRUE) show_city,COALESCE(pp.show_online,TRUE) show_online,COALESCE(pp.birth_visibility,'age') birth_visibility,COALESCE(pp.who_can_message,'friends') who_can_message,COALESCE(pp.who_can_see_posts,'everyone') who_can_see_posts,COALESCE(pp.who_can_see_friends,'friends') who_can_see_friends FROM users u LEFT JOIN user_locations l ON l.user_id=u.id LEFT JOIN profile_privacy pp ON pp.user_id=u.id WHERE LOWER(u.username)=$1 AND u.account_status='active'`,[uname])).rows[0];if(!p)return res.status(404).json({ok:false,error:"USER_NOT_FOUND"});const rel=await relation(viewer.id,p.id),canPosts=await allowed(p.who_can_see_posts,viewer.id,p.id),canFriends=await allowed(p.who_can_see_friends,viewer.id,p.id),canMessage=await allowed(p.who_can_message,viewer.id,p.id),friendCount=canFriends?Number((await pool.query(`SELECT COUNT(*)::int c FROM friendships WHERE status='accepted' AND(requester_id=$1 OR addressee_id=$1)`,[p.id])).rows[0].c):null,postCount=canPosts?Number((await pool.query(`SELECT COUNT(*)::int c FROM posts WHERE user_id=$1 AND deleted_at IS NULL`,[p.id])).rows[0].c):0,online=p.show_online?Boolean(await redis.zScore("presence:users",String(p.id)).then(s=>s&&Number(s)>Date.now()-120000).catch(()=>false)):false;let posts=[];if(canPosts){const rows=(await pool.query(`${postSql} AND p.user_id=$2 ORDER BY p.id DESC LIMIT 30`,[viewer.id,p.id])).rows;posts=await filterVisible(rows,viewer.id)}res.json({ok:true,profile:{...p,last_seen_at:p.show_last_seen?p.last_seen_at:null,city:p.show_city?p.city:null,birth_date:undefined,friends_count:friendCount,posts_count:postCount,relation:rel,can_message:canMessage,online},posts})}catch(e){console.error("FGH regression profile",e);res.status(500).json({ok:false,error:"PROFILE_LOAD_FAILED"})}});
 }
 return prior(app,...args)
};
