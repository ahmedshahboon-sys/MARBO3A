import http from "http";
import crypto from "crypto";
import {pool,redis,ensureRedis,requireAuth,tokenFrom,isAdmin,clean} from "./runtime.mjs";

const previous=http.createServer.bind(http),SESSION_TTL=7*24*60*60;
const hash=t=>crypto.createHash("sha256").update(String(t)).digest("hex");
async function recoverSession(req){await ensureRedis();const t=tokenFrom(req);if(!t)return null;let id=await redis.get(`session:${t}`);if(!id){const row=(await pool.query(`SELECT user_id,expires_at FROM durable_sessions WHERE token_hash=$1 AND expires_at>NOW()`,[hash(t)])).rows[0];if(row){id=String(row.user_id);const ttl=Math.max(60,Math.min(SESSION_TTL,Math.floor((new Date(row.expires_at).getTime()-Date.now())/1000)));await redis.set(`session:${t}`,id,{EX:ttl})}}if(id)await pool.query(`UPDATE durable_sessions SET last_seen=NOW(),expires_at=GREATEST(expires_at,NOW()+INTERVAL '7 days') WHERE token_hash=$1`,[hash(t)]).catch(()=>{});return id}
async function auth(req,res){await recoverSession(req);return requireAuth(req,res)}
async function roomRole(roomId,userId){return(await pool.query(`SELECT role FROM room_members WHERE room_id=$1 AND user_id=$2`,[roomId,userId])).rows[0]?.role||null}

http.createServer=function v1CompletionCreateServer(app,...args){
 if(typeof app==="function"&&app?.use){
   ensureRedis().then(()=>pool.query(`DELETE FROM durable_sessions WHERE expires_at<NOW()`)).catch(e=>console.error("v1 init",e));

   app.use(async(req,_res,next)=>{try{if(tokenFrom(req))await recoverSession(req)}catch(e){console.error("session recovery",e.message)}next()});

   // Keep every issued session durable so Redis/container restarts do not log users out.
   app.use((req,res,next)=>{const authResponse=req.method==="POST"&&["/api/auth/login","/api/auth/register","/api/auth/verify-2fa"].includes(req.path);if(!authResponse)return next();const original=res.json.bind(res);res.json=data=>{if(data?.token&&data?.user?.id){ensureRedis().then(()=>pool.query(`INSERT INTO durable_sessions(token_hash,user_id,expires_at,last_seen) VALUES($1,$2,NOW()+INTERVAL '7 days',NOW()) ON CONFLICT(token_hash) DO UPDATE SET user_id=EXCLUDED.user_id,expires_at=EXCLUDED.expires_at,last_seen=NOW()`,[hash(data.token),data.user.id])).catch(e=>console.error("persist session",e.message))}return original(data)};next()});
   app.use((req,res,next)=>{if(req.method==="POST"&&req.path==="/api/auth/logout"){const t=tokenFrom(req);res.on("finish",()=>{if(t)pool.query(`DELETE FROM durable_sessions WHERE token_hash=$1`,[hash(t)]).catch(()=>{})})}next()});

   app.get("/api/search",async(req,res)=>{try{const u=await auth(req,res);if(!u)return;const q=clean(req.query.q,80);if(q.length<2)return res.json({ok:true,users:[],rooms:[],posts:[]});const like=`%${q}%`;const[users,rooms,posts]=await Promise.all([pool.query(`SELECT id,username,display_name,avatar_url,bio FROM users WHERE account_status='active' AND id<>$1 AND (username ILIKE $2 OR display_name ILIKE $2) ORDER BY display_name LIMIT 20`,[u.id,like]),pool.query(`SELECT id,name,slug,description,(SELECT COUNT(*)::int FROM room_members m WHERE m.room_id=rooms.id) members_count FROM rooms WHERE is_public=TRUE AND (name ILIKE $1 OR description ILIKE $1) ORDER BY id DESC LIMIT 20`,[like]),pool.query(`SELECT p.id,p.body,p.image_url,p.created_at,x.username,x.display_name,x.avatar_url FROM posts p JOIN users x ON x.id=p.user_id WHERE p.deleted_at IS NULL AND p.body ILIKE $1 ORDER BY p.id DESC LIMIT 20`,[like])]);res.json({ok:true,users:users.rows,rooms:rooms.rows,posts:posts.rows})}catch(e){console.error("global search",e);res.status(500).json({ok:false,error:"SEARCH_FAILED"})}});

   app.post("/api/chats/:id/read",async(req,res)=>{try{const u=await auth(req,res);if(!u)return;const id=Number(req.params.id),conv=(await pool.query(`SELECT 1 FROM direct_conversations WHERE id=$1 AND (user1_id=$2 OR user2_id=$2)`,[id,u.id])).rows[0];if(!conv)return res.status(403).json({ok:false,error:"FORBIDDEN"});const last=(await pool.query(`SELECT id FROM direct_messages WHERE conversation_id=$1 ORDER BY id DESC LIMIT 1`,[id])).rows[0]?.id||null;await pool.query(`INSERT INTO direct_message_reads(conversation_id,user_id,last_read_message_id,last_read_at) VALUES($1,$2,$3,NOW()) ON CONFLICT(conversation_id,user_id) DO UPDATE SET last_read_message_id=EXCLUDED.last_read_message_id,last_read_at=NOW()`,[id,u.id,last]);res.json({ok:true,lastReadMessageId:last})}catch{res.status(500).json({ok:false,error:"READ_UPDATE_FAILED"})}});

   app.get("/api/chats/:id/receipt",async(req,res)=>{try{const u=await auth(req,res);if(!u)return;const id=Number(req.params.id),conv=(await pool.query(`SELECT user1_id,user2_id FROM direct_conversations WHERE id=$1 AND (user1_id=$2 OR user2_id=$2)`,[id,u.id])).rows[0];if(!conv)return res.status(403).json({ok:false,error:"FORBIDDEN"});const peer=String(conv.user1_id)===String(u.id)?conv.user2_id:conv.user1_id,r=(await pool.query(`SELECT last_read_message_id,last_read_at FROM direct_message_reads WHERE conversation_id=$1 AND user_id=$2`,[id,peer])).rows[0]||null;res.json({ok:true,receipt:r})}catch{res.status(500).json({ok:false,error:"RECEIPT_FAILED"})}});

   app.get("/api/rooms/:id/members",async(req,res)=>{try{const u=await auth(req,res);if(!u)return;const id=Number(req.params.id);if(!await roomRole(id,u.id)&&!isAdmin(u))return res.status(403).json({ok:false,error:"NOT_ROOM_MEMBER"});const rows=(await pool.query(`SELECT m.user_id,m.role,m.joined_at,x.username,x.display_name,x.avatar_url,x.last_seen_at,EXISTS(SELECT 1 FROM room_bans b WHERE b.room_id=m.room_id AND b.user_id=m.user_id) banned FROM room_members m JOIN users x ON x.id=m.user_id WHERE m.room_id=$1 ORDER BY CASE m.role WHEN 'owner' THEN 0 WHEN 'moderator' THEN 1 ELSE 2 END,m.joined_at`,[id])).rows;res.json({ok:true,members:rows})}catch{res.status(500).json({ok:false,error:"ROOM_MEMBERS_FAILED"})}});

   app.post("/api/rooms/:id/members/:userId/moderate",async(req,res)=>{try{const u=await auth(req,res);if(!u)return;const roomId=Number(req.params.id),target=Number(req.params.userId),action=clean(req.body?.action,20),reason=clean(req.body?.reason,160),role=await roomRole(roomId,u.id),admin=isAdmin(u);if(!admin&&!["owner","moderator"].includes(role))return res.status(403).json({ok:false,error:"FORBIDDEN"});const targetRole=await roomRole(roomId,target);if(targetRole==="owner"&&!admin)return res.status(403).json({ok:false,error:"OWNER_PROTECTED"});if(action==="promote"){if(role!=="owner"&&!admin)return res.status(403).json({ok:false,error:"OWNER_ONLY"});await pool.query(`UPDATE room_members SET role='moderator' WHERE room_id=$1 AND user_id=$2`,[roomId,target])}else if(action==="demote"){if(role!=="owner"&&!admin)return res.status(403).json({ok:false,error:"OWNER_ONLY"});await pool.query(`UPDATE room_members SET role='member' WHERE room_id=$1 AND user_id=$2`,[roomId,target])}else if(action==="kick"){await pool.query(`DELETE FROM room_members WHERE room_id=$1 AND user_id=$2 AND role<>'owner'`,[roomId,target])}else if(action==="ban"){await pool.query(`INSERT INTO room_bans(room_id,user_id,created_by,reason) VALUES($1,$2,$3,$4) ON CONFLICT(room_id,user_id) DO UPDATE SET created_by=EXCLUDED.created_by,reason=EXCLUDED.reason,created_at=NOW()`,[roomId,target,u.id,reason]);await pool.query(`DELETE FROM room_members WHERE room_id=$1 AND user_id=$2 AND role<>'owner'`,[roomId,target])}else if(action==="unban"){await pool.query(`DELETE FROM room_bans WHERE room_id=$1 AND user_id=$2`,[roomId,target])}else return res.status(400).json({ok:false,error:"INVALID_ACTION"});res.json({ok:true,action})}catch(e){console.error("room moderation",e);res.status(500).json({ok:false,error:"ROOM_MODERATION_FAILED"})}});

   app.use(async(req,res,next)=>{if(req.method==="POST"&&/^\/api\/rooms\/\d+\/join$/.test(req.path)){try{const u=await auth(req,res);if(!u)return;const roomId=Number(req.path.split("/")[3]);if((await pool.query(`SELECT 1 FROM room_bans WHERE room_id=$1 AND user_id=$2`,[roomId,u.id])).rows[0])return res.status(403).json({ok:false,error:"ROOM_BANNED"})}catch(e){return next(e)}}next()});

   app.post("/api/rooms/:id/pin/:messageId",async(req,res)=>{try{const u=await auth(req,res);if(!u)return;const roomId=Number(req.params.id),messageId=Number(req.params.messageId),role=await roomRole(roomId,u.id);if(!isAdmin(u)&&!["owner","moderator"].includes(role))return res.status(403).json({ok:false,error:"FORBIDDEN"});if(!(await pool.query(`SELECT 1 FROM messages WHERE id=$1 AND room_id=$2 AND deleted_at IS NULL`,[messageId,roomId])).rows[0])return res.status(404).json({ok:false,error:"MESSAGE_NOT_FOUND"});await pool.query(`UPDATE rooms SET pinned_message_id=$1 WHERE id=$2`,[messageId,roomId]);res.json({ok:true,pinnedMessageId:messageId})}catch{res.status(500).json({ok:false,error:"PIN_FAILED"})}});

   app.get("/api/v1/status",async(req,res)=>{try{const u=await auth(req,res);if(!u)return;const[posts,rooms,users,online]=await Promise.all([pool.query(`SELECT COUNT(*)::int c FROM posts WHERE deleted_at IS NULL`),pool.query(`SELECT COUNT(*)::int c FROM rooms`),pool.query(`SELECT COUNT(*)::int c FROM users WHERE account_status='active'`),pool.query(`SELECT COUNT(*)::int c FROM visitor_presence WHERE last_seen>NOW()-INTERVAL '2 minutes'`)]);res.json({ok:true,version:"1.0-readiness",counts:{posts:posts.rows[0].c,rooms:rooms.rows[0].c,users:users.rows[0].c,online:online.rows[0].c},features:{durableSessions:true,globalSearch:true,readReceipts:true,roomModeration:true,roomPins:true}})}catch{res.status(500).json({ok:false,error:"STATUS_FAILED"})}});
 }
 return previous(app,...args)
};
